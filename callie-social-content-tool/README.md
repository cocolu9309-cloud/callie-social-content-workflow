# Callie 社媒内容生成工具

## 快速启动

```bash
# 1. 安装依赖
pip install -r requirements.txt

# 2. 配置 API Key
编辑 config.yaml，填入 siliconflow_api_key

# 3. 启动服务
uvicorn app:app --reload --port 8000

# 4. 打开浏览器
http://localhost:8000
```